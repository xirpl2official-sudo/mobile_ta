<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Row Content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingVertical="@dimen/spacing_text"
        android:gravity="center_vertical">

        <!-- Waktu & Tanggal -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center"
            android:paddingStart="@dimen/spacing_medium">

            <TextView
                android:id="@+id/tvWaktu"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="07:30"
                android:textSize="@dimen/text_riwayat_waktu"
                android:textColor="#666666"
                android:gravity="center" />

            <TextView
                android:id="@+id/tvTanggal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="14 NOV 2024"
                android:textSize="@dimen/text_riwayat_tanggal"
                android:textStyle="bold"
                android:textColor="#000000"
                android:gravity="center"
                android:layout_marginTop="@dimen/spacing_tiny" />

        </LinearLayout>

        <!-- Nama Sholat -->
        <TextView
            android:id="@+id/tvSholat"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Dhuha"
            android:textSize="@dimen/text_riwayat_sholat"
            android:textColor="#000000"
            android:gravity="center" />

        <!-- Status Badge -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center">

            <TextView
                android:id="@+id/tvStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Hadir"
                android:textSize="@dimen/text_riwayat_status"
                android:textColor="#FFFFFF"
                android:background="@drawable/bg_status_hadir"
                android:paddingHorizontal="@dimen/riwayat_padding_horizontal"
                android:paddingVertical="@dimen/riwayat_padding_vertical"
                android:gravity="center" />

        </LinearLayout>

    </LinearLayout>

    <!-- Divider Line -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#E0E0E0" />

</LinearLayout>