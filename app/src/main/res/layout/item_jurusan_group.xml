<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardJurusanGroup"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:strokeColor="#E2E8F0"
    app:strokeWidth="1dp"
    app:cardBackgroundColor="@color/surface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Jurusan Header -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/jurusanHeader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:background="?attr/selectableItemBackground">

            <ImageView
                android:id="@+id/ivJurusanLogo"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_class"
                android:contentDescription="Logo Jurusan"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent" />

            <TextView
                android:id="@+id/tvJurusanName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="RPL"
                android:textColor="@color/on_background"
                android:textSize="18sp"
                android:textStyle="bold"
                android:layout_marginStart="12dp"
                app:layout_constraintStart_toEndOf="@id/ivJurusanLogo"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/tvKelasCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_status_gray"
                android:paddingHorizontal="8dp"
                android:paddingVertical="2dp"
                android:text="3 Kelas"
                android:textColor="@color/on_background"
                android:textSize="12sp"
                app:layout_constraintStart_toEndOf="@id/tvJurusanName"
                app:layout_constraintTop_toTopOf="@id/tvJurusanName"
                app:layout_constraintBottom_toBottomOf="@id/tvJurusanName" />

            <ImageView
                android:id="@+id/ivExpandGroup"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:contentDescription="Perluas"
                android:src="@drawable/ic_chevron_right"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:tint="#94A3B8" />

        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- Expandable: inner RecyclerView for kelas items -->
        <LinearLayout
            android:id="@+id/layoutExpandableGroup"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="12dp"
            android:paddingBottom="12dp"
            android:visibility="gone">

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/divider"
                android:layout_marginBottom="8dp" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvKelasInGroup"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:nestedScrollingEnabled="false" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
